這個資料夾放「白牌九圖」Hero 主圖的標準版型參考圖。

檔名固定為 hero-ref-1.png（若是 JPG 也請沿用 .png 這個檔名，或改用 .jpg 並同步改 NineCard.jsx 的下載路徑）。
前端 Hero 卡片的「⬇ 下載標準版型參考圖」按鈕會抓 /assets/hero-ref-1.png。

換圖：直接覆蓋這個檔案（同檔名）即可，push 後 Cloudflare 會自動重新部署。
